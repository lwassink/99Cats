<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h2>Rent a Cat!</h2>

    <form action= <%= cat_rental_requests_url %> method="post">
      <label>
        Pick a cat:
        <select name="rental[cat_id]">
          <option value= <%= nil %>>(select cat)</option>
          <% @cats.each do |cat| %>
            <option value= <%= cat.id %> > <%= cat.name %> </option>
          <% end %>
        </select>
      </label>

      <br>
      <label>
        Start date:
        <input type="date" name="rental[start_date]">
      </label>

      <label>
        End date:
        <input type="date" name="rental[end_date]">
      </label>
      <button>Submit</button>
    </form>


  </body>
</html>
